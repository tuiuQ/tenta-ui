export default {
  mounted: function (el: Event, binding: any, vnode: any): void {
    // const type = binding.value.teType
    // const theme = binding.value.teTheme ? binding.value.teTheme : 'outlined'
    // console.log(type);
    // console.log(theme);

  }
}
